{
  "name": "@kit/web3-react",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Durable pending-transaction registry and contract-write wrapper with cross-tab ownership, phased toasts, and reconciliation",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0"
  },
  "peerDependencies": {
    "@tanstack/react-query": "^5.0.0",
    "react": "^18.0.0 || ^19.0.0",
    "viem": "^2.0.0",
    "wagmi": "^2.0.0"
  },
  "dependencies": {
    "@kit/contract": "workspace:*",
    "@kit/core": "workspace:*",
    "@kit/web3": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
