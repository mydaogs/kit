{
  "name": "@kit/kv",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared Redis client with lazy env resolution, safe to load inside Next runtime infrastructure",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./rest": "./src/rest.ts"
  },
  "files": ["src"],
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint ."
  },
  "peerDependencies": { "@upstash/redis": "^1.36.0" },
  "devDependencies": { "typescript": "^5.7.3" }
}
